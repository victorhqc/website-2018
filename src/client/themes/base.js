import {
  SIZES,
  PADDING,
  MARGIN,
  FONT_SIZES,
  FONT_FAMILIES,
  COLORS,
} from './constants';

const baseTheme = {
  fontFamily: FONT_FAMILIES.roboto,
  fontSize: FONT_SIZES.sm,

  constants: {
    SIZES,
    PADDING,
    MARGIN,
    FONT_SIZES,
    FONT_FAMILIES,
    COLORS,
  },
};

export default baseTheme;
